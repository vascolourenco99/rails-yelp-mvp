<h1>Restaurants</h1>
<p><%= link_to "Create a New Restaurant", new_restaurant_path %></p>
<ul>
    <% @restaurants.each do |restaurant| %>
    <ol><%= link_to restaurant.name, restaurant_path(restaurant)%></ol>
    <% end %>
</ul>
